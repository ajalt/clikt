subprojects {
    apply plugin: 'kotlin'
    apply plugin: 'application'

    dependencies {
        implementation project(':clikt')
    }

    compileKotlin {
        kotlinOptions.jvmTarget = "1.8"
    }
}
