subprojects {
    apply plugin: 'kotlin'
    apply plugin: 'application'

    dependencies {
        implementation implementation("com.github.ajalt:clikt:2.5.0")
    }

    compileKotlin {
        kotlinOptions.jvmTarget = "1.8"
    }
}
