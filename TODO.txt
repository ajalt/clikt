store all data on the context to allow parsers to be pure.
http://click.pocoo.org/5/commands/#group-invocation-without-command

option receiver on ValueProcessor
remaining option types
custom help formatter

examples
    cp (Consider --interactive)
    required options
    argparse style help formatter
    custom multi commands

```
$ ./repo clone --help
Usage: cli [OPTIONS] COMMAND [ARGS]...

Error: no such option --help
```
```
.default("").int() shouldn't be allowed
```
wrapping is wrong:
```
Usage: commit [OPTIONS] [FILES]...

Commits outstanding changes.

 Commit changes to the given files into the repository. You will need to "repo
push" to push up your changes to other repositories.

 If a list of files is omitted, all changes reported by "repo status" will be
committed.
```
should be:
```
Usage: cli.py [OPTIONS]

  ok boys

  let's do some stuff let's do some stuff let's do some stuff let's do some
  stuff let's do some stuff

  it'll be great
```
